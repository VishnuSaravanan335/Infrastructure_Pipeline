# Infrasture_pipeline
Infrastructure‑as‑Code project using Terraform to provision a complete AWS  architecture. It automates VPC networking, EC2 compute with Auto Scaling, Application Load Balancer, DynamoDB for data storage, and S3 for object storage — all managed through remote state in S3.
